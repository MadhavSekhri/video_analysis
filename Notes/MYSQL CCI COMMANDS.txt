MYSQL CCI COMMANDS
mysql -u root -p

SHOW DATABASES;

CREATE DATABASE cci_bng_new;
EXIT;

mysql -u root -p cci_bng_new < "F:\maddy\cci_bng_dump.sql"

mysql -u root -p cci_bng_new < "F://maddy//cci_bng_dump.sql"


USE cci_bng_new;
SHOW TABLES;

watch "mysql -u root -p -e 'SHOW TABLE STATUS FROM cci_bng_new;'"

mysql -u root -p cci_bng < /f/maddy/cci_bng_dump.sql




yoYoHoney$1ngh
